import { FunctionComponent } from "react";

export type ProductProps = {
    limit?: number;
};

export const Product: FunctionComponent<ProductProps> = ({ limit }: ProductProps ) => {
    return (
        <>
        </>
    )
}